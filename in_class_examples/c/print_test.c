#include <stdio.h>

int main(){
  unsigned char y = 0;
  printf("y is %d\n", y);
  //y = y - 1;
  printf("y is %d\n", y - 1);
  unsigned int x = (unsigned int) -1;
  printf("x is %u\n", x);
  double result = 3 / 2.0;
  printf("result is %f\n", result);
  return 0;
}
